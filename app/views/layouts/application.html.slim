html
  head
    title
      'TestAppUniqsystems
    = csrf_meta_tags

    = stylesheet_link_tag    'application', media: 'all'


  body
    nav.navbar.navbar-inverse
      .container
        #navbar.navbar-collapse.collapse
          ul.nav.navbar-nav
            li
              a href="/" Main

          ul.nav.navbar-nav.navbar-right
            - if current_user.present?
              li
                p.navbar-text = current_user.name
              li = link_to 'Sign out', destroy_user_session_path, target: '_self'
            - else
              li = link_to 'Sing in', new_user_session_path, target: '_self'
              li = link_to 'Sing up', new_user_registration_path, target: '_self'
    .container
      - if flash[:alert].present?
        p.alert.alert-danger.text-center = flash[:alert]
      = yield
    = javascript_include_tag 'application'